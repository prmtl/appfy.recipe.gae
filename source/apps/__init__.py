from tipfy import REQUIRED_CONFIG

#: Default configuration values for this module. Keys are:
#: - ``sitename``: Name of the site, used by default in some places. Default
#:   is ``tipfy.REQUIRED_CONFIG``.
#: - ``admin_email``: Administrator e-mail. Default is
#:   ``tipfy.REQUIRED_CONFIG``.
default_config = {
    'sitename':    REQUIRED_CONFIG,
    'admin_email': REQUIRED_CONFIG,
}
